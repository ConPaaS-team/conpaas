#---
<% if @vm_info.has_elements?('OS') %>
        OS = [
        <% first = true %>

        <% @vm_info.each('OS') do |os| %>
                <% if os.attr('TYPE', 'arch') %>
                        arch = "<%= os.attr('TYPE', 'arch').split('/').last %>" 
                        <% first = false %>
                <% end %>
        <% end %>
 
        <% @vm_info.each('OS/*') do |cont| %>
            <% if (cont.name != 'TYPE') and cont.text %>
            <% if first %>
                    <%= cont.name %> = "<%= cont.text %>"
                    <% first = false %>
                <% else %>
                    ,<%= cont.name %> = "<%= cont.text %>"
                <% end %>
                <% end %>
        <% end %>
        ]
<% end %>

GRAPHICS = [type="vnc",listen="0.0.0.0",port="-1"]
#----    
