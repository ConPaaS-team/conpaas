To create a tarball for this svn copy, run the mkpackage.sh script inside this directory (trunk).

This script requires one argument, i.e. the name of the release.

Example:
  
   ./mkpackage.sh ConPaaS-1.0.0-rc1

   You will obtain the archive ConPaaS-1.0.0-rc1.tar.gz inside this directory 
