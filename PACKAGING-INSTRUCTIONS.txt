To create a tarball for this svn copy, run the mkdist.sh script inside this
directory (trunk).

This script requires one argument, i.e. the release number.

Example:
  
   ./mkdist.sh 1.1.0-rc1

You will obtain the ConPaaS tarballs inside this directory 
